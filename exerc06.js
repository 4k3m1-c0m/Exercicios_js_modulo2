// Crie uma variável chamada contador e atribua o valor 0. Em seguida, incremente seu valor três vezes e exiba o resultado no console.
let contador = 0;

contador++; // Incrementa para 1
contador++; // Incrementa para 2
contador++; // Incrementa para 3

console.log(contador);
//Como o comando foi executado três vezes, o valor exibido no console será 3

// Declare uma variável chamada altura sem atribuir um valor a ela. Em seguida, exiba seu valor no console. O que acontece e por quê?
let altura;
console.log(altura);

// Valor no console: Undefined
// Por quê ?: A variável existe, mas ela ainda não tem conteúdo